import { InputStylesDirective } from './input-styles.directive';

describe('InputStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new InputStylesDirective();
    expect(directive).toBeTruthy();
  });
});
