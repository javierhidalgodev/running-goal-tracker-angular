import { OnButtonSpinnerDirective } from './on-button-spinner.directive';

describe('OnButtonSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new OnButtonSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
