import { DaysToEndPipe } from './days-to-end.pipe';

describe('DaysToEndPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysToEndPipe();
    expect(pipe).toBeTruthy();
  });
});
