import { GoalPercentagePipe } from './goal-percentage.pipe';

describe('GoalPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new GoalPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
